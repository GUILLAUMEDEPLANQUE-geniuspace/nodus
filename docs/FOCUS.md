# Porte produit — sans retirer la puissance

Nodus pose le monde. Ghost l’habite. V7/V8 mesurent, staff.

## Accueil

Une phrase : *Tu crées un univers. Tu poses les lieux et les preuves. Un hôte n’en sort pas.*

Home = cette porte + cinq germes + Vera / Lumen en **témoins**. Pas le produit.

`/create` ouvre sur les germes (`atelier-anime`, `terrain`, `vault`, `maison-rh`, `manga-hub`). Les 10 flagships et ~50 démarrages + Finance (crypto, immo, banque, droit) restent dans les onglets.

Bibliothèque : salles + types de fiches à cocher. Pack = preset. Une fiche n’existe que publiée et unique.

## Hôte public

`GET|POST /n/{slug}/ghost` : `GhostLite::reply` puis `GhostHost::publicSurface`.
`Ghost::reply` / `GhostCore::think` restent entiers (staff : lab, gym, cerveau, editor, maturity).

## Cluster SEO

`/n/{slug}/sitemap.xml` → `SeoCompiler::sitemapXml`.
`/n/{slug}/llms.txt` → `SeoCompiler::llmsTxt`.
`/n/{slug}/schema.json` fusionne `SeoCompiler::worldGraph` + RoomSchema.
Index seulement hub + salles avec contenu + fiches `EntityFloor` (copie ≥ 80, Jaccard sœur < 0.72).

Partage inter-mondes = composants, jamais le lore.

## Interdit de casser

- Aucun flagship retiré
- Aucune classe Ghost* effacée
- Engine ⇏ Ghost / Grantor / Chrome
- I-NO-ACT, rideau, plancher, tribunal factuel
- GhostDream `applied = false`
